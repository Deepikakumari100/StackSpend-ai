
  # Fix audit functionality issues

  This is a code bundle for Fix audit functionality issues. The original project is available at https://www.figma.com/design/ODaPM9CBhfVAC3KsxSJAtN/Fix-audit-functionality-issues.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  